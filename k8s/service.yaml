apiVersion: v1
kind: Service
metadata:
  name: machine-test-api
spec:
  selector:
    app: machine-test-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
  # TODO: Change to LoadBalancer if you want external access directly
  # type: LoadBalancer
