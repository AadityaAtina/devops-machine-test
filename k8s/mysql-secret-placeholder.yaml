# IMPORTANT: Do NOT commit real credentials here.
# This is a placeholder showing the structure only.
# In production, use Azure Key Vault with Managed Identity or Kubernetes External Secrets Operator.
#
# TODO: Replace placeholder values with actual base64-encoded values
# To encode: echo -n 'yourvalue' | base64
#
# Create the secret by running:
# kubectl create secret generic mysql-secret \
#   --from-literal=host=<your-mysql-host> \
#   --from-literal=username=<your-db-user> \
#   --from-literal=password=<your-db-password> \
#   --from-literal=database=<your-db-name> \
#   -n app

apiVersion: v1
kind: Secret
metadata:
  name: mysql-secret
  namespace: app
type: Opaque
data:
  host: REPLACE_WITH_BASE64_HOST
  username: REPLACE_WITH_BASE64_USERNAME
  password: REPLACE_WITH_BASE64_PASSWORD
  database: REPLACE_WITH_BASE64_DATABASE
